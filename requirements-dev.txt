# Host-side tooling for Robox firmware development.
# Nothing here is installed on the Pico.
#
#   python3 -m pip install --user -r requirements-dev.txt

# Official MicroPython remote-control CLI. Backs every task in
# .vscode/tasks.json via tools/pico.
mpremote>=1.24.0

# NOTE: the MicroPython type stubs are NOT listed here on purpose.
# They must land in ./typings (where pyrightconfig.json points stubPath), not
# in site-packages, so they are installed with --target instead:
#
#   ./tools/pico stubs
#
# Board target is the plain RP2040 Pico: micropython-rp2-rpi_pico-stubs.
# A Pico W would need micropython-rp2-rpi_pico_w-stubs.
