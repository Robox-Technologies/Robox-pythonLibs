# Host-side tooling for Robox firmware development.
# Nothing here is installed on the Pico.
#
#   python3 -m pip install --user -r requirements-dev.txt

# Official MicroPython remote-control CLI. Backs every task in
# .vscode/tasks.json via tools/pico.
mpremote>=1.24.0

# Type stubs so Pylance understands `machine`, `_thread`, `ustruct`, `utime`.
# Optional: the MicroPico extension ships its own copy into
# .vscode/Pico-W-Stub. Install these if you want stubs without the extension,
# then add the site-packages path to python.analysis.extraPaths.
micropython-rp2-rpi_pico-stubs>=1.24.0
